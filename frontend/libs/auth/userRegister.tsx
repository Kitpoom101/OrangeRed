import { NextResponse } from "next/server";
import { dbConnect } from "@/db/dbConnect";
import User from "@/db/models/Users";

export async function POST(req: Request) {
  ///use handle register directly instead naja jub jub <3
}